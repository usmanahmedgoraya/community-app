// styles/fonts.js
import { Inter_Tight, Asap } from '@next/font/google';

export const interTight = Inter_Tight({
  weight: ['400', '700'],  // Specify the weights you need
  subsets: ['latin'],      // Specify the subsets you need
});
export const asap = Asap({
  weight: ['400', '700'],  // Specify the weights you need
  subsets: ['latin'],      // Specify the subsets you need
});
